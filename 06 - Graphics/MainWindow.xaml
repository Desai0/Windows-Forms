<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>

        <LinearGradientBrush x:Key="PrimaryBrush" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="#FF6A82FB" Offset="0.0" />
            <GradientStop Color="#FFFC5C7D" Offset="1.0" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="HeaderBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="LightBlue" Offset="0"/>
            <GradientStop Color="Blue" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="FancyButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="DarkSlateGray" 
                                BorderThickness="1" 
                                CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.4" BlurRadius="5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="DarkSlateBlue"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="DarkOrange"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ItemTemplate">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="10" Height="10" Fill="Green" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding}" />
            </StackPanel>
        </DataTemplate>

    </Window.Resources>

    <DockPanel LastChildFill="True">

        <Border Background="{StaticResource HeaderBrush}" Height="50" DockPanel.Dock="Top">
            <TextBlock Text="WPF Пример" 
                       FontSize="20" 
                       Foreground="White" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Width="200" Background="LightGray" DockPanel.Dock="Right">
            <Button Style="{StaticResource FancyButtonStyle}" Margin="5" Content="Кнопка 1"/>
            <Button Style="{StaticResource FancyButtonStyle}" Margin="5" Content="Кнопка 2"/>
        </StackPanel>

        <Grid Background="White">

            <Canvas>
                <Rectangle Width="100" Height="100" Fill="LightCoral" Canvas.Right="20" Canvas.Top="30"/>
                <Ellipse Width="80" Height="80" Fill="LightGreen" Canvas.Left="260" Canvas.Top="160"/>
            </Canvas>

            <ListBox x:Name="listBox1"
                     Width="200"
                     Height="100"
                     Margin="10"
                     ItemTemplate="{StaticResource ItemTemplate}"
                     VerticalAlignment="Bottom"
                     HorizontalAlignment="Left">
            </ListBox>

        </Grid>

    </DockPanel>
</Window>